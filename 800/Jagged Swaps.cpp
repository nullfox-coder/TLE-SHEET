#include <bits/stdc++.h>
using namespace std;

class Solution {
    
};

int main() {
    int t;
    cin>>t;
    for(int test=0;test<t;test++)
    {
       int n, first;
       cin>>n;
       cin>>first;
       for(int i=1;i<n;i++)
       {
            int x;
            cin>>x;
       }
       cout<<(first==1 ? "YES" : "NO")<<endl;
    }
    return 0;
}